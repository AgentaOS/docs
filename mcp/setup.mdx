---
title: MCP Server
description: Connect an AI agent to AgentaOS so it can run your store from chat.
---

The AgentaOS MCP server lets any Model Context Protocol client (Claude Desktop, Cursor, or Windsurf) create checkouts, read your customers and subscriptions, and send receipts on your behalf. Use it when you want your agent to run the store directly, instead of calling the API or CLI by hand.

## Setup

Add the server to your client's config with a gateway key from [app.agentaos.ai](https://app.agentaos.ai) → **Settings → Developers → API Keys**:

```json
{
  "mcpServers": {
    "agenta": {
      "command": "npx",
      "args": ["-y", "agentaos"],
      "env": {
        "AGENTAOS_GATEWAY_KEY": "sk_test_your-gateway-key"
      }
    }
  }
}
```

Test keys (`sk_test_`) only ever see test data, so your agent can build and break things safely. The same config works for Cursor and Windsurf.

## Tools

| Tool | What it does |
|------|-------------|
| `agenta_pay_create_checkout` | Create a checkout and get a shareable `checkoutUrl`. |
| `agenta_pay_get_checkout` | Check whether a payment completed. |
| `agenta_pay_list_checkouts` | List checkouts by status (`open`, `completed`, `expired`, `cancelled`). |
| `agenta_pay_list_subscriptions` | List subscriptions: status, amount, interval, and subscriber. |
| `agenta_pay_cancel_subscription` | Cancel a subscription, at period end or immediately. |
| `agenta_pay_list_customers` | Everyone who paid you: email, name, and country. |
| `agenta_pay_send_receipt` | Re-send the receipt email for a paid invoice. |

## Next steps

<CardGroup cols={2}>
  <Card title="Run your store from an agent" icon="wand-magic-sparkles" href="/guides/ai-agent">
    The full walkthrough: a coding agent with the CLI, or a chat agent with MCP.
  </Card>
  <Card title="TypeScript SDK" icon="node-js" href="/sdk/pay-overview">
    The same operations as typed methods in `@agentaos/pay`.
  </Card>
  <Card title="CLI" icon="terminal" href="/cli/install">
    Run AgentaOS from your terminal with `agenta`.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/getting-started/quickstart">
    Create a product and take your first payment.
  </Card>
</CardGroup>
